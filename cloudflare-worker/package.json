{
  "name": "ikdx-auth-worker",
  "version": "1.0.0",
  "description": "iKDx authentication service using Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "@tsndr/cloudflare-worker-jwt": "^2.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
